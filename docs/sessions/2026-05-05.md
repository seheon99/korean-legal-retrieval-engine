# Session 2026-05-05 — scope pending

## State at session start

- ADR-001 through ADR-014 accepted.
- Phase-1 statute schema is frozen at
  [migrations/001_statute_tables.sql](../../migrations/001_statute_tables.sql)
  per ADR-010, with accepted follow-up decisions from ADR-013 and ADR-014:
  - ADR-013 replaces `is_current` with `is_head` and separates latest
    ingested version from legal effectiveness.
  - ADR-014 defines `annexes` ingestion plus parallel
    `annex_attachments` / `form_attachments`; no `attachment_blobs` in
    Phase 1.
- `legal_documents` ingestion is implemented, idempotent, and DB-verified.
  ADR-009 Act-to-Decree parent lookup is working against the Phase-1 corpus.
- `structure_nodes` ingestion is implemented, tested, and backfilled into
  the running Docker Compose `database` service:
  2 `legal_documents`, 240 normalized `structure_nodes`, 232 child rows
  with `parent_id`.
- `structure_nodes.number` stores normalized citation numbers; original
  punctuation-bearing legal text remains in `content`.
- Working tree clean at session start.
- Latest commit at session start:
  `7d57ac4 docs: rename agent instructions`.

## Carried-forward threads

1. **ADR-014 annex ingestion** — recommended next implementation thread.
   The Phase-1 Decree sample has 5 appendices; annexes are the remaining
   statute-side chunk source. Parse `<별표>` / `<별표단위>` into `annexes`
   and persist HWP/PDF/image references into `annex_attachments`.
2. **First additive migration shape** — needed before applying ADR-013
   column rename and ADR-014 attachment tables. Migration tool selection
   should be settled before `002_*.sql` becomes precedent.
3. **Forms ingestion** — lower priority than annexes because the Phase-1
   corpus currently has 0 form rows; keep `form_attachments` schema aligned
   with ADR-014, but parser work can wait.
4. **ADR-006 defensive boundary** — ministry-prefixed `doc_type` trigger
   remains unimplemented.
5. **Packaging** — `pyproject.toml`, pinned dependencies, pytest/ruff, and
   host-side execution without `PYTHONPATH=src`.

## Scope confirmation

Seheon requested ADR-014 implementation for `annexes` +
`annex_attachments`, including the first additive migration after choosing
the migration shape.

Migration shape is an ADR-level precedent before `002_*.sql` ships.
Drafted [ADR-015](../decisions/ADR-015-migration-management.md) and
accepted it after review.

Recommendation in ADR-015:

- keep migrations as ordered raw PostgreSQL SQL files under `migrations/`
- add a `schema_migrations` ledger
- make `002_annex_form_attachments.sql` additive:
  `schema_migrations`, `annex_attachments`, `form_attachments`, indexes,
  CHECK constraints
- keep the ADR-013 `is_current` -> `is_head` rename as a separate
  non-additive migration

Implementation proceeding under accepted ADR-015.

## What happened

Implemented ADR-014 for `annexes` and `annex_attachments`.

Migration:

- Accepted [ADR-015](../decisions/ADR-015-migration-management.md).
- Added
  [migrations/002_annex_form_attachments.sql](../../migrations/002_annex_form_attachments.sql):
  - bootstraps `schema_migrations`
  - records baseline `001_statute_tables.sql`
  - creates `annex_attachments`
  - creates `form_attachments`
  - adds owner/type indexes and `attachment_type IN ('hwp', 'pdf', 'image')`
    CHECK constraints
- Applied `002` to the running Compose `database`; ledger now records
  `001,002`.

Code:

- [src/ingest/records.py](../../src/ingest/records.py) — added
  `Annex` and `AnnexAttachment` pydantic records.
- [src/ingest/parse.py](../../src/ingest/parse.py) — added
  `parse_annexes()` and `parse_annex_attachments()` with ADR-014
  validation:
  - `별표키` shape `^[0-9]{6}[EF]$`
  - `별표` rows require `E`; `서식` rows require `F`
  - key number/branch segments must agree with `<별표번호>` /
    `<별표가지번호>`
  - duplicate `annex_key` halts before DB insertion
  - annex title and normalized `<별표내용>` are required
  - HWP/PDF source URLs are preserved verbatim
  - image filenames are preserved in XML order with no source URL
- [src/ingest/populate.py](../../src/ingest/populate.py) —
  `_insert_children()` now inserts `structure_nodes`, then `annexes`, then
  `annex_attachments` using an in-memory `annex_key -> annex_id` map.

Tests:

- Added [tests/ingest/test_annexes.py](../../tests/ingest/test_annexes.py).
- Parser checks cover:
  - Act sample yields 0 annexes / 0 attachments
  - Decree sample yields 5 annexes
  - Decree annex keys are `000100E` through `000500E`
  - numbers normalize to `1` through `5`
  - branch numbers are `NULL`
  - 21 attachment-reference rows: 5 HWP, 5 PDF, 11 image
  - binary storage fields remain `NULL`
  - malformed key/discriminator/number/title/content cases halt ingestion

Verification:

| Check | Result |
|-------|--------|
| `python -m compileall src/ingest` | OK |
| `PYTHONPATH=src python -m pytest tests/ingest/test_structure_nodes.py tests/ingest/test_annexes.py -q` | 13 passed |
| Apply `002_annex_form_attachments.sql` to running Compose DB | OK |
| Backfill existing Decree row | 5 `annexes`, 21 `annex_attachments` |
| Phase-1-only idempotent re-ingest | skipped both existing docs by `content_hash`; counts unchanged |

Running Compose DB state after backfill:

| Table / relation | Count |
|------------------|-------|
| `legal_documents` | 2 |
| `structure_nodes` | 240 |
| `structure_nodes.parent_id IS NOT NULL` | 232 |
| `annexes` | 5 |
| `annex_attachments` | 21 |
| `form_attachments` | 0 |

Next action:

1. Implement ADR-013 `is_current` -> `is_head` rename migration and code
   update. This is now unblocked by ADR-015 but intentionally separate
   from the additive ADR-014 migration.
2. Implement persistence-only `supplementary_provisions` ingestion per
   ADR-005.

## Parking lot

- Phase-2 migration: drop redundant `structure_nodes.sort_key`.
- TODO-2 / TODO-5 / TODO-7 remain additive Phase-2 work.
- Chunk generation and retrieval-pipeline code remain out of scope until the
  source ingestion layer is complete enough to index.
