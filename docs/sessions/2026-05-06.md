# Session 2026-05-06 — ADR-013 `is_head` rename

## State at session start

- Local session date confirmed with `date +%F`: `2026-05-06`.
- ADR-001 through ADR-018 accepted.
- Phase-1 statute schema is frozen at
  [migrations/001_statute_tables.sql](../../migrations/001_statute_tables.sql)
  per ADR-010, with accepted amendments through ADR-018.
- Post-freeze migration management is established by ADR-015:
  ordered raw SQL migrations plus `schema_migrations`.
- Last verified source-ingestion state from the 2026-05-05 session close:
  - Phase-1 Act and Decree are ingested.
  - `structure_nodes` ingestion is implemented and tested.
  - `annexes` and `annex_attachments` ingestion is implemented and tested.
  - Compose DB held 5 annex rows and 21 attachment-provenance rows.
  - ADR-017 binary retention policy is PDF-default with HWP fallback:
    5 PDF files retained locally; HWP/image rows remain provenance-only.
  - ADR-018 annex `content_text` de-layouting is implemented for the
    Phase-1 corpus.
- ADR-013 is accepted but still not applied in code/DDL:
  `migrations/001_statute_tables.sql` and
  [src/ingest/populate.py](../../src/ingest/populate.py) still contain
  `is_current`.
- Working tree clean at session start.
- Branch at session start: `main`.
- Latest commit at session start:
  `0fa04cd docs(session): close 2026-05-05 annex session`.

## Carried-forward threads

1. Implement the ADR-013 `is_current` -> `is_head` rename migration and
   code update. This is accepted naming debt still present in executable
   DDL/code.
2. Decide the concrete Korean tokenizer / morphological analyzer for the
   ADR-018 production boundary scorer before replacing the current
   reviewed deterministic Phase-1 substitute.
3. Implement persistence-only `supplementary_provisions` ingestion per
   ADR-005.
4. Move toward chunk generation for `structure_nodes` and `annexes` after
   the source-ingestion layer is stable enough to index.

## Scope confirmation

Seheon requested creation of today's session file only. No implementation
scope has been selected yet.

Follow-up scope selected: implement carried-forward thread 1, the accepted
ADR-013 `is_current` -> `is_head` rename migration and executable code
update.

## What happened

Implemented the ADR-013 executable rename thread.

Migration:

- Added
  [migrations/003_is_head_rename.sql](../../migrations/003_is_head_rename.sql).
- The migration:
  - renames `is_current` to `is_head` on `legal_documents`
  - renames `is_current` to `is_head` on `structure_nodes`
  - renames `is_current` to `is_head` on `annexes`
  - renames `is_current` to `is_head` on `forms`
  - renames `ux_legal_documents_current_act_title` to
    `ux_legal_documents_head_act_title`
  - records `003` in `schema_migrations`
- Kept [migrations/001_statute_tables.sql](../../migrations/001_statute_tables.sql)
  unchanged per ADR-015. The frozen baseline still contains `is_current`;
  clean databases converge by applying `001`, `002`, then `003`.

Code:

- Updated [src/ingest/populate.py](../../src/ingest/populate.py):
  - parent Act lookup now predicates on `is_head = TRUE`
  - `legal_documents`, `structure_nodes`, and `annexes` inserts write
    `is_head`
  - failure wording now says "head Act" instead of "current Act"

Docs:

- Updated [docs/phase-1-progress.md](../phase-1-progress.md) so the
  next-session guide no longer points at the completed rename thread.

## Verification

Local checks:

| Check | Result |
|-------|--------|
| `python -m compileall src/ingest scripts/download_annex_attachments.py scripts/refresh_annex_content.py` | OK |
| `PYTHONPATH=src python -m pytest tests/ingest/test_structure_nodes.py tests/ingest/test_annexes.py tests/test_download_annex_attachments.py -q` | 17 passed |

Existing Compose DB:

| Check | Result |
|-------|--------|
| Apply `003_is_head_rename.sql` | OK |
| `schema_migrations` | `001`, `002`, `003` |
| temporal flag columns | `is_head` present on all 4 source tables; `is_current` absent |
| head Act title index | `ux_legal_documents_head_act_title`, predicate uses `is_head = true` |
| source row counts | 2 `legal_documents`, 240 `structure_nodes`, 5 `annexes`, 21 `annex_attachments` |
| attachment retention | PDF 5 stored/checksummed/fetched; HWP 0; image 0 |

Clean-path verification:

- Created throwaway database `kle_verify_adr013`.
- Applied migrations `001`, `002`, `003`.
- Ran Phase-1-only ingest against Act + Decree XML.
- Verified:
  - 2 `legal_documents`
  - 240 `structure_nodes`
  - 232 child `structure_nodes` with `parent_id`
  - 5 `annexes`
  - 21 `annex_attachments`
  - Decree `parent_doc_id = 1`
  - both document rows have `is_head = TRUE`
  - idempotent rerun skipped both MSTs by matching `content_hash`
- Dropped the throwaway database after verification.

Note:

- A broad `docker compose run --rm ingest` against all local
  `data/raw` still halts on the known non-Phase-1 `형법` heading
  generalization issue: duplicate `structure_nodes.node_key '0001000'`.
  The phase transaction rolled back; main DB counts stayed unchanged.

## Carried-forward threads after this work

1. Implement persistence-only `supplementary_provisions` ingestion per
   ADR-005.
2. Decide the concrete Korean tokenizer / morphological analyzer for the
   ADR-018 production boundary scorer.
3. Move toward chunk generation for `structure_nodes` and `annexes` after
   the source-ingestion layer is stable enough to index.

## Follow-up — Phase 1 OSH expansion planning

Seheon changed the Phase-1 roadmap: `산업안전보건법` must be added to
Phase 1.

Planning decisions selected:

- Phase-1 horizon: full roadmap from current ingestion through first
  retrieval baseline.
- OSH coverage: full family — Act, 시행령, and 시행규칙.
- Version policy: ingest both head/future-effective and effective-as-of
  slices when they differ.
- First retrieval baseline: hybrid immediately, not BM25-only.
- Embedding model policy: compare `bge-m3` and KURE before freezing model
  choice.

Created:

- [docs/phase-1-osh-roadmap.md](../phase-1-osh-roadmap.md)
- [docs/decisions/ADR-019-phase-1-osh-scope.md](../decisions/ADR-019-phase-1-osh-scope.md)
  with status `Proposed`.

Per the ADR-first agreement, implementation must pause here until Seheon
explicitly approves ADR-019. After approval, flip ADR-019 to `Accepted`
before changing fetch scripts, parser code, migrations, or retrieval code.

## Follow-up — ADR-019 approved

Seheon explicitly approved ADR-019 on 2026-05-06.

Updated:

- [docs/decisions/ADR-019-phase-1-osh-scope.md](../decisions/ADR-019-phase-1-osh-scope.md)
  status changed from `Proposed` to `Accepted`.
- [docs/phase-1-osh-roadmap.md](../phase-1-osh-roadmap.md) now treats the
  SAPA + OSH full-family scope as accepted.
- [docs/phase-1-progress.md](../phase-1-progress.md) and
  [AGENTS.md](../../AGENTS.md) no longer describe Phase 1 as SAPA-only.

Next executable step:

1. Run OSH law.go.kr API discovery for Act, 시행령, and 시행규칙 using
   `lawSearch.do` / `lawService.do` with `target=law` and `target=eflaw`.
2. Record exact `law_id`, `mst`, title, `doc_type`, effective date, and
   head/effective-slice interpretation.
3. Stop and draft a raw-retention identity ADR if `target=eflaw` returns
   distinct XML for the same `(law_id, mst)` at different `efYd` values.

## Follow-up — OSH API discovery hit ADR-019 stop condition

Initial law.go.kr discovery was run with the local `.env` OC value, without
printing or persisting the OC.

Current `target=law` search rows:

| Title | law_id | MST | type | effective date |
| --- | --- | --- | --- | --- |
| `산업안전보건법` | `001766` | `276853` | `법률` | `20251001` |
| `산업안전보건법 시행령` | `003786` | `284771` | `대통령령` | `20260324` |
| `산업안전보건법 시행규칙` | `007364` | `271485` | `고용노동부령` | `20260101` |

Critical findings:

- `lawService.do?target=law&MST=271485` returns OSH Rule XML with
  `<법종구분>고용노동부령</법종구분>` and `<시행일자>20250601</시행일자>`;
  adding `efYd=20260101` to `target=law` does not change that XML.
- `lawService.do?target=eflaw` returns distinct XML for the same `MST`
  when `efYd` changes:
  - OSH Act `MST=283449`: `20260601`, `20260801`, `20270101` produced
    three distinct hashes.
  - OSH Decree `MST=263363`: `20240701`, `20250101`, `20260626`
    produced three distinct hashes.
  - OSH Rule `MST=263749`: `20240701`, `20250101`, `20260626` produced
    three distinct hashes.
  - OSH Rule `MST=271485`: `20250601`, `20260101` produced two distinct
    hashes.

Per ADR-019, ingestion must stop here. Drafted
[ADR-020](../decisions/ADR-020-effective-law-raw-identity.md) with status
`Proposed`.

Next step: Seheon must review ADR-020. Do not write OSH raw files,
migrations, parser changes, or ingestion rows until ADR-020 is accepted.

## Follow-up — ADR-020 revision

Seheon revised the ADR-020 direction: do not model `law` and `eflaw` as
equal canonical document sources.

Updated [ADR-020](../decisions/ADR-020-effective-law-raw-identity.md) to
make `target=eflaw` the proposed canonical statute XML source for Phase 1.

Key revision points:

- Canonical raw identity: `(law_id, mst, efYd)`.
- Canonical ingested identity: `(law_id, mst, effective_date)`.
- Canonical raw path:
  `data/raw/eflaw/{law_id}/{mst}/{efYd}.xml`.
- `target=law` is auxiliary only: discovery, fallback, debugging,
  comparison, or legacy verification.
- Existing `data/raw/{law_id}/{mst}.xml` files are legacy data to remove
  or replace with verified equivalent `eflaw` slices.

ADR-020 remains `Proposed`. No implementation until approval.

## Follow-up — ADR-020 identity precision

Seheon clarified ADR-020 identity wording:

- Retained raw XML identity for canonical rows is
  `(target=eflaw, law_id, mst, efYd)`.
- Ingested document identity remains `(law_id, mst, effective_date)`.
- `target` is fixed to `eflaw` for canonical Phase-1 statute XML, so it
  does not belong in the `legal_documents` uniqueness rule.
- `target=eflaw` remains raw-source provenance and must be recorded in
  `source_url`.

Updated ADR-020 accordingly. It remains `Proposed`.

## Follow-up — ADR-020 approved

Seheon approved ADR-020 on 2026-05-06.

Updated:

- [docs/decisions/ADR-020-effective-law-raw-identity.md](../decisions/ADR-020-effective-law-raw-identity.md)
  status changed from `Proposed` to `Accepted`.
- [docs/phase-1-progress.md](../phase-1-progress.md) and
  [AGENTS.md](../../AGENTS.md) now point to ADR-020 implementation.

Next executable work:

1. Add migration `004` replacing `UNIQUE(mst)` with
   `UNIQUE(law_id, mst, effective_date)`.
2. Fetch canonical `eflaw` XML under
   `data/raw/eflaw/{law_id}/{mst}/{efYd}.xml`.
3. Replace or remove legacy `target=law` SAPA raw XML only after verified
   equivalent `eflaw` slices exist.

## Follow-up — ADR-020 executable slice

Implemented and verified the first ADR-020 executable slice.

Changed:

- Added
  [migrations/004_eflaw_identity.sql](../../migrations/004_eflaw_identity.sql),
  replacing `UNIQUE(mst)` with
  `UNIQUE(law_id, mst, effective_date)` on `legal_documents`.
- Updated `parse.discover()` to discover canonical
  `data/raw/eflaw/{law_id}/{mst}/{efYd}.xml` files, while direct
  `parse_doc()` calls can still parse legacy files for transitional tests.
- Updated `parse_doc()` for canonical `eflaw` paths:
  - derives `law_id` from `법령ID` path segment
  - derives `mst` from the parent directory
  - validates path `efYd` against XML `<시행일자>`
  - writes non-OC `source_url` with `target=eflaw` and `efYd`
- Updated idempotency in `populate.py` from `mst` to
  `(law_id, mst, effective_date)`.
- Updated [scripts/fetch_law_samples.sh](../../scripts/fetch_law_samples.sh)
  to fetch canonical `target=eflaw` XML.
- Added parser tests for ADR-020 identity behavior.
- Updated README raw-response instructions.

Fetched canonical SAPA `eflaw` files:

- `data/raw/eflaw/013993/228817/20220127.xml`
- `data/raw/eflaw/014159/277417/20251001.xml`

Verification:

| Check | Result |
| --- | --- |
| `bash -n scripts/fetch_law_samples.sh` | OK |
| `PYTHONPATH=src python -m pytest tests/ingest/test_effective_law_identity.py tests/ingest/test_structure_nodes.py tests/ingest/test_annexes.py tests/test_download_annex_attachments.py -q` | 20 passed |
| Apply `004_eflaw_identity.sql` to running Compose DB | OK |
| Running Compose DB `schema_migrations` | `001`, `002`, `003`, `004` |
| Clean throwaway DB migrations `001`-`004` + canonical `eflaw` ingest | OK |
| Clean throwaway counts | 2 `legal_documents`, 240 `structure_nodes`, 5 `annexes`, 21 `annex_attachments` |

Important note:

- Canonical `eflaw` file hashes differ from legacy `target=law` file
  hashes. Existing legacy DB rows should not be silently updated in place;
  clean rebuild or an explicit replacement step is required.

## Follow-up — OSH canonical `eflaw` fetch

Fetched the ADR-019 OSH canonical `eflaw` files under `data/raw/eflaw`.

OSH Act:

- `data/raw/eflaw/001766/276853/20251001.xml`
- `data/raw/eflaw/001766/283449/20260601.xml`
- `data/raw/eflaw/001766/283449/20260801.xml`
- `data/raw/eflaw/001766/285379/20261208.xml`
- `data/raw/eflaw/001766/283449/20270101.xml`

OSH Enforcement Decree:

- `data/raw/eflaw/003786/284771/20260324.xml`
- `data/raw/eflaw/003786/263363/20260626.xml`

OSH Enforcement Rule:

- `data/raw/eflaw/007364/271485/20260101.xml`
- `data/raw/eflaw/007364/263749/20260626.xml`

Verification:

- `find data/raw/eflaw -type f` now shows 11 canonical files: 9 OSH
  files plus 2 SAPA files.
- `parse_doc()` succeeds for OSH Act and Enforcement Decree files.
- Initial `parse_doc()` verification halted on OSH Enforcement Rule because
  `<법종구분>` is `고용노동부령`, not one of the literal ADR-006 DB enum
  values. The correction below supersedes this as an ADR-006 blocker.

Next executable step after the correction: continue OSH source-ingestion gaps
for supersession, supplementary provisions, forms, and form attachments.

Correction during the 2026-05-06 night session:

- Seheon pointed to 법제처's explanation that `대통령령` corresponds to
  `시행령`, and `총리령ㆍ부령` correspond to `시행규칙`.
- Therefore OSH Rule's `고용노동부령` is not an ADR-006 taxonomy blocker.
  It is an ingestion normalization gap.
- Implemented normalization from ministry-prefixed `*부령` to canonical
  DB `doc_type='부령'`, while retaining the original value in raw XML and
  `법종구분코드` in `doc_type_code`.

## Follow-up — OSH source ingestion gaps

Continued the same 2026-05-06 night session and implemented the OSH
source-layer ingestion gaps that blocked full canonical `eflaw` loading.

Implemented:

- Structure parser now handles inline branched `<호번호>` values such as
  `3의2.` when `<호가지번호>` is absent. This keeps ADR-012's accepted
  branch-numbering scope and fixes duplicate `node_key` failures in OSH
  Act, Decree, and Rule XML.
- Added persistence-only `supplementary_provisions` parsing/insertion per
  ADR-004 / ADR-005.
- Added metadata-only `forms` parsing/insertion and `form_attachments`
  provenance insertion for OSH Rule forms.
- Implemented ADR-013 source-row lifecycle:
  - `effective_at` is set at `00:00:00 Asia/Seoul` from
    `effective_date`.
  - Incoming absent source rows supersede the previous head row in the
    same `law_id` lineage.
  - Old `structure_nodes`, `annexes`, and `forms` under superseded
    `doc_id` values are marked `is_head = FALSE` with matching
    `superseded_at`.
  - `supplementary_provisions` remains version-scoped by `doc_id` and has
    no temporality update, per ADR-013.
- Ingest order within each doc-type phase is now chronological by
  `effective_date`, then `law_id`, then `mst`, so retained future slices
  supersede in temporal order.

Verification:

| Check | Result |
| --- | --- |
| Parse all 11 canonical `eflaw` files for doc / structure / 부칙 / annex / forms | OK |
| `PYTHONPATH=src python -m pytest tests/ingest/test_structure_nodes.py tests/ingest/test_annexes.py tests/ingest/test_effective_law_identity.py tests/ingest/test_supplementary_and_forms.py tests/test_download_annex_attachments.py -q` | 24 passed |
| Clean throwaway DB `osh_ingest_smoke_20260506_night`, migrations `001`-`004`, full canonical `eflaw` ingest | OK |
| Re-run ingest against same throwaway DB | OK; all 11 source rows skipped by hash |

Clean throwaway DB counts after full canonical ingest:

| Table | Rows |
| --- | ---: |
| `legal_documents` | 11 |
| `structure_nodes` | 11,384 |
| `supplementary_provisions` | 141 |
| `annexes` | 129 |
| `annex_attachments` | 774 |
| `forms` | 222 |
| `form_attachments` | 712 |

Remaining boundary:

- Default Compose DB still needs an explicit replacement posture because
  it was previously loaded from legacy `target=law` SAPA rows.
- Rule `parent_doc_id` remains the ADR-009 revisit trigger. Do not infer
  Act-vs-Decree parentage for 시행규칙 without a follow-up ADR.

## Follow-up — Default Compose DB rebuild

Seheon chose rebuild as the default DB replacement posture.

Executed:

1. `docker compose down -v`
2. `docker compose up -d database`
3. `docker compose run --rm ingest`
4. `docker compose run --rm ingest` again for idempotency verification

Result:

- The default Compose `pgdata` volume was dropped and recreated.
- Postgres init applied migrations `001`, `002`, `003`, and `004`.
- Canonical `data/raw/eflaw` ingest populated the default DB from scratch.
- The second ingest run skipped all 11 canonical source rows by
  `content_hash`.
- `legal_documents.source_url` verification showed zero non-`eflaw` rows.

Default Compose DB counts after rebuild:

| Table | Rows |
| --- | ---: |
| `legal_documents` | 11 |
| `structure_nodes` | 11,384 |
| `supplementary_provisions` | 141 |
| `annexes` | 129 |
| `annex_attachments` | 774 |
| `forms` | 222 |
| `form_attachments` | 712 |

Current remaining boundary:

- Binary attachment retention metadata was reset with the DB rebuild.
  Source attachment provenance is present; durable `stored_file_path` /
  checksum fields require a new download pass if needed.
- Rule `parent_doc_id` remains the ADR-009 revisit trigger.
