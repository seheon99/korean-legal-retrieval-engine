# Session 2026-04-26 — ADR-003 accepted; Fedora handoff

## Decisions

| ID | Decision | Resolves |
|----|----------|----------|
| ADR-003 | `chunks` uses split FK columns with real `REFERENCES`, generated `source_type`, `ON DELETE RESTRICT`, 1:N chunks-per-source | implicit assumption inside ADR-002 |

## Other changes

- ADR-002 conditional language collapsed to the actual ADR-003 outcome
  (`structure_node_id BIGINT`, `annex_id BIGINT` FK columns on chunks).
  The phase-1-progress.md polymorphic sketch is now `superseded`.

## Machine handoff — Fedora

Closing early to migrate to the Fedora server (IP-whitelisted for the
법제처 OpenAPI). On Fedora, `docs/api-samples/` is gitignored and starts
empty; re-fetch the three Phase-1 samples (Act `MST=228817`, Decree
`MST=277417`, search `query=중대재해`) per `README.md`.

## Open threads

ERD TODOs unchanged: TODO-2, 3, 5, 6, 7, 8, 9, 10. Recommended next:
TODO-9 → ADR-004.
