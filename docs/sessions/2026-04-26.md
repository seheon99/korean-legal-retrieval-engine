# Session 2026-04-26 — ADR-003 accepted, prep for Fedora handoff

## What happened

Short session, single decision finalized.

1. **ADR-003 (chunks FK shape) accepted** — split FK columns with real
   `REFERENCES` clauses, generated `source_type` column, `ON DELETE
   RESTRICT`, 1:N chunks-per-source granularity assumption recorded inline.

2. **ADR-002 conditional language collapsed.** Six places in ADR-002 that
   were drafted in "whichever shape ADR-003 picks" form during the
   proposal phase were rewritten to describe the actual outcome
   (`structure_node_id BIGINT`, `annex_id BIGINT` FK columns on chunks).
   The phase-1-progress.md polymorphic sketch is now noted as
   **superseded** rather than "proposed to supersede."

3. **Committed as `ee18d8e`.** Working tree clean before machine handoff.

## Decisions finalized

| ID | Decision | Resolves |
|----|----------|----------|
| ADR-003 | Chunks table uses split FK columns with real FKs, generated `source_type`, 1:N granularity assumption, `ON DELETE RESTRICT` | Implicit assumption inside ADR-002 |

## Still open

ERD-side TODOs (no change from yesterday's count): TODO-2, 3, 5, 6, 7, 8,
9, 10. The yesterday-recommended next thread remains **TODO-9 (부칙
placement)** — small and isolated, candidate for **ADR-004 (Proposed)**.

## Machine handoff — Fedora server

Closing this session early to migrate to the Fedora server, which is
IP-whitelisted for the 법제처 OpenAPI. Implications for the next session
on Fedora:

- The current working machine **cannot fetch fresh API samples** (IP
  blocked). Fedora can.
- `docs/api-samples/` is gitignored. On Fedora it will be empty initially
  — re-fetch via the curl commands in `README.md` once `LAW_GO_KR_OC` is
  exported.
- Recommended fetch on arrival: re-create the three Phase 1 samples
  (Act `MST=228817`, Decree `MST=277417`, search `query=중대재해`).
  These are the inputs the ERD draft and ADRs 1/2/3 were derived from.
- A real fetch script (`scripts/fetch_law_samples.sh`) is overdue and
  could be the second item in the next session — it converts the
  README's curl block into a versioned, idempotent, parameterizable
  utility.

## Next session starting point (on Fedora)

1. **Verify environment**: `git pull`, `export LAW_GO_KR_OC=<value>`,
   re-fetch the three samples per README.
2. **Sanity check** the samples round-trip cleanly (e.g., the Act XML
   still parses and the 별표 5 entries from yesterday are still there
   in the Decree).
3. **Draft ADR-004 (Proposed)** for TODO-9 — 부칙 placement (separate
   `supplementary_provisions` table vs merging into `structure_nodes`
   with a sentinel `level` value).
4. **Optional**: extract the README's fetch curl into
   `scripts/fetch_law_samples.sh`.

## Artefacts produced this session

- `docs/decisions/ADR-003-chunks-fk-shape.md` (status flipped to Accepted)
- `docs/decisions/ADR-002-identifier-strategy.md` (conditional language
  collapsed to ADR-003 outcome)
- `docs/sessions/2026-04-26.md` (this file)
