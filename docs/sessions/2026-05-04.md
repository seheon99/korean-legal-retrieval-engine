# Session 2026-05-04 — structure_nodes parser depth + ADR-013/014

## Decisions

| ID | Decision |
|----|----------|
| ADR-013 | Amendment tracking / supersession policy. Replaces `is_current` with `is_head`; legal effectiveness derived from `effective_at` / `superseded_at`. Same-MST `content_hash` mismatch remains a hard data-integrity error. |
| ADR-014 | Annex ingestion contract for `<별표단위>`. Parallel `annex_attachments` / `form_attachments` keep source provenance separate from optional application-owned storage paths. No `attachment_blobs` layer in Phase 1. HWP/PDF/image attachment types. |

## Code landed

`_insert_children()` now inserts `structure_nodes` from the `<조문>`
block:

- Parser ([src/ingest/parse.py](../../src/ingest/parse.py)):
  `parse_structure_nodes(doc)`, ADR-012 key helpers, text/date/hash
  helpers, ADR-012 verification triggers.
- Records ([src/ingest/records.py](../../src/ingest/records.py)): frozen
  `StructureNode` model with parser-only `parent_node_key` for
  deterministic FK resolution before insert.
- Orchestrator ([src/ingest/populate.py](../../src/ingest/populate.py)):
  `_insert_children()` inserts in parse order, resolves `parent_id` from
  previously returned `node_id`s. 부칙/별표/서식 explicitly out of scope
  in this thread.
- Tests:
  [tests/test_structure_nodes.py](../../tests/test_structure_nodes.py)
  covers ADR-012 key composition, real-sample counts, duplicate
  `node_key` prevention, parent-key resolvability.

Parser behavior:

- `조문여부=전문` → level 2; `조문여부=조문` → level 5; nested
  `<항>`/`<호>`/`<목>` → 6/7/8.
- Bare `<항>` materializes as implicit level-6 row (`number=''`,
  `content=''`, key segment `00`).
- `node_key` / `sort_key` follow ADR-012 tagless format.
- `content_hash` is SHA-256 of node content text.

## Follow-up — normalized `structure_nodes.number`

Seheon flagged that `number` mixed normalized article numbers (`4`) with
raw XML display tokens for 호/목 (`4.`, `가.`). Corrected:

- `number` now stores the canonical citation number (no trailing
  punctuation); future branched 호 → `7의2` when XML has
  `<호번호>7.</호번호>` + `<호가지번호>2</호가지번호>`.
- 항 circled numbers (`①`, `②`, …) and implicit-항 empty string
  unchanged.
- `content` is untouched; original legal text still begins with `4.` /
  `가.` where the API emitted that text.

Live DB UPDATE affected 151 rows (`level IN (7, 8)` with trailing-dot
`number`).

## Empirical findings worth preserving

- 형법 exposes duplicate `조문키='0001000'` for multiple `전문` heading
  rows — outside Phase-1 SAPA heading assumption; needs handling when
  generalizing heading levels beyond 중대재해처벌법.

## DB-verified counts after this session

| Table | Rows |
| ----- | ---: |
| `legal_documents` | 2 |
| `structure_nodes` | 240 (level-8 목 = 66) |
| `structure_nodes.parent_id IS NOT NULL` | 232 |

| doc_id | mst | title | parent_doc_id | structure_nodes |
| ------ | --- | ----- | ------------- | --------------- |
| 1 | 228817 | 중대재해 처벌 등에 관한 법률 | NULL | 102 |
| 2 | 277417 | 중대재해 처벌 등에 관한 법률 시행령 | 1 | 138 |

## Open threads

1. ADR-014 implementation for `annexes` + `annex_attachments`
   (Decree has 5 appendices).
2. Forms ingestion (lower priority — Phase-1 corpus has 0 form rows).
3. First additive migration shape — needed before applying ADR-013
   column rename and ADR-014 attachment tables. Migration tool selection
   should be settled before `002_*.sql` becomes precedent.
4. ADR-006 ministry-prefixed `doc_type` defensive boundary.
5. Packaging — `pyproject.toml`, pinned deps, pytest/ruff.
